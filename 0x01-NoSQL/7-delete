// script that deletes all docs with the name ALX School in the collection school
db.students.remove({ name: 'ALX School' }, { justOne: false }, (err, result) => {
    if (err) {
        console.error('Error deleting documents:', err);
    } else {
        console.log('Documents deleted:', result.deletedCount);
    }
});
